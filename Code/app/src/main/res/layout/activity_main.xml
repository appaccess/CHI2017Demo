<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#2e85a6"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <ImageButton
        android:id="@+id/doge"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:src="@drawable/doge"
        android:contentDescription="doge"
        android:textColor="#FFF" />

    <Button
        android:id="@+id/resource_name_of_button"
        android:contentDescription="doge"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginTop="10dp"
        android:layout_below="@+id/hidden"
        android:background="#34495e"
        android:textColor="#FFF" />



    <Button
        android:id="@+id/activateAccessibilityButton"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_below="@+id/button2"
        android:layout_marginTop="150dp"
        android:background="#34495e"
        android:text="Activate Accessibility Service"
        android:textColor="#FFF" />

    <Button
        android:id="@+id/openAppButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/activateAccessibilityButton"
        android:layout_marginTop="10dp"
        android:background="#34495e"
        android:text="Screencast"
        android:textColor="#FFF" />

</LinearLayout>
